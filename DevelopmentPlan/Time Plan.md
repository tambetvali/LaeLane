Resolution:
- https://github.com/tambetvali/LaeLane/blob/main/DevelopmentPlan/Design%20Switch.md

Explains it all.
- Learn new languages like Red and Rebol - takes a little time.
- Implement Laegna number system in way which does not collapse into standard sugar for decimals,
  - It's much more easy if one can just write laegna numbers and wave numbers, in standard language, not in growing complexity layer of virtual semantics.

Javscript version will also be replaced:

***CoPilot>***

Short answer: **Rebol can run in the browser today**, and **Red will be able to**, but not yet.

Rebol **can run client‑side** through **JS/HTML interpreters** created by the community.

***<CoPilot>***

---

The lesson I learned was tough:
- That KISS and simplicity needs redesign right now to simpler standard number type representation of Laegna.
- It's yet computational, but providing basic digit-playing needs already in mathulus a calc can do.

For this lesson, I take "time off" - now some documents and specifications, development plans might appear, but I recode critical parts and turn this into one python-js interface, starting from python so js interface can be mocked by server client.
