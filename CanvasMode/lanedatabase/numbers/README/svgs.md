This was the task for SVG generator, implicitly based on previous task in pngs.md, for png file generator:

Perfect: can you now create class with same features and implementation - SVG should also support black, transparent, or white background with black or white line, so all 4 combinations should be possible; same calls to draw points are used. Do not inherit, but simply let's assume if I simply use the other class initializer, I could copy the whole code and only the file extension differs - file naming rules remain the same.

So write an SVG version, where only class name and file extension, or similarly basic things differ, but the syntax remains and code is more reusable altough not equal.